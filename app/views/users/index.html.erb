<%= provide(:title, 'Inventar | All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
	<li>
      <%= link_to "#{user.vorname} #{user.nachname}", user %>
    </li>
  <% end %>
</ul>
